# @ncleton-petitmaker/yaka-bridge-runtime

Facade package for Bridge daemon/runtime behavior.

The implementation remains in `bridge/` while the platform is being extracted.
Client projects consume the package contract and must not vendor runtime files.
