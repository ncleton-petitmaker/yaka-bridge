# @ncleton-petitmaker/yaka-bridge-voice

Facade package for the local push-to-talk sidecar.

The Rust sidecar source remains in `bridge-voice/`. Client projects must use the
published Bridge desktop package and must not copy this sidecar source.
