# @ncleton-petitmaker/yaka-bridge-desktop

Facade package for the Bridge desktop runtime and packaging surface.

The source still lives in `bridge/` and `electron-builder.bridge.cjs` during the
monorepo transition. Client projects must depend on this package instead of
copying Bridge desktop files.
